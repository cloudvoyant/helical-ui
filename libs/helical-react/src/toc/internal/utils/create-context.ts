// libs/helical-react/src/toc/internal/utils/create-context.ts
// Ark UI's context helper. Ark publishes this exact implementation from
// `@ark-ui/react/utils`, so this file is a thin re-export that preserves the
// vendored folder's import shape (`./utils/create-context`) without duplicating
// upstream logic. See ../README.md for the stable-swap instructions.
export { createContext } from '@ark-ui/react/utils';
