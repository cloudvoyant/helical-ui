---
title: Installation
description: Install helical-ui packages with pnpm.
order: 2
---

## Workspace packages

The library ships three npm packages:

- `@cloudvoyant/helical-ui` — shared interfaces, cva variants, and the shadcn-style theme.
- `@cloudvoyant/helical-react` — React components (Ark UI based).
- `@cloudvoyant/helical-svelte` — Svelte components (Ark UI based).

## Install

```bash
pnpm add @cloudvoyant/helical-ui @cloudvoyant/helical-react
# or for Svelte
pnpm add @cloudvoyant/helical-ui @cloudvoyant/helical-svelte
```

Import the theme once in your app's global CSS:

```css
@import 'tailwindcss';
@import '@cloudvoyant/helical-ui/theme.css';
/* Registers the helical-ui component class strings (cva variants + framework
   classes) with Tailwind via @source self-scanning — no manual @source needed. */
@import '@cloudvoyant/helical-ui/components.css';
```
